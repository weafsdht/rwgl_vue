# 前端 API 基础地址（可选）
# 不设置时使用相对路径 /api，依赖 Vite 代理或同源部署。
# 当后端在 192.168.1.220:8080、前端从别的设备/浏览器访问时，设置为（注意带 /api）：
# VITE_API_BASE_URL=http://192.168.1.220:8080/api
# 配置后请先访问 http://192.168.1.220:8080/api/health 确认能通，再试登录和看板。

# VITE_API_BASE_URL=http://192.168.1.220:8080/api
